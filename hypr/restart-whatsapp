#!/usr/bin/env bash
set -euo pipefail

"$(dirname -- "${BASH_SOURCE[0]}")/whatsapp-companion-close" 2>/dev/null || true
sleep 0.5
setsid omarchy-launch-webapp 'https://web.whatsapp.com/' >/dev/null 2>&1 &
